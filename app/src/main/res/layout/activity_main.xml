<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="16dp"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/textViewDescribeFormula"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Food Intake - Exercise Calories Burned = Calories Required* \n\n(*Based on your Health Target)"/>

        <ProgressBar
            android:id="@+id/progressBar"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="100dp"
            android:layout_margin="16dp"
            android:progressDrawable="@drawable/custom_progress"
            android:indeterminate="false"
            android:layout_gravity="center"
            android:visibility="visible"
            android:progressTint="@android:color/holo_green_light"
            />

    <TextView
        android:id="@+id/textViewSuggestionTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Optimizing Your Wellness:"
        android:textStyle="bold"/>
    <TextView
        android:id="@+id/textViewSuggestion"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Suggestion"/>

</LinearLayout>